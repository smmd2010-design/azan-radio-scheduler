# Copy this file to .env and adjust if needed. Most configuration
# (stream URL, duration, devices, prayer-time source) is done from the
# web admin UI after first run, not here - this file only covers
# settings needed before the app can even start.

# Container/system timezone. The app's own scheduling timezone is set
# separately in the Settings page (defaults to Asia/Singapore too).
TZ=Asia/Singapore
